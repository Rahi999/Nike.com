const initState = {
    loginData : [],
    loginLoading : false,
    loginError : false
}

export const reducer = (state=initState,{type,payload}) => {
    switch(type) {
        default : return state;
    }
}